import { BoxCoordinatesPipe } from './box-coordinates.pipe';

describe('BoxCoordinatesPipe', () => {
  it('create an instance', () => {
    const pipe = new BoxCoordinatesPipe();
    expect(pipe).toBeTruthy();
  });
});
